my_breakfast <- c("oatmeal", "banana")
meals <- list(my_breakfast, my_lunch)
meals <- list(my_breakfast, my_lunch)
# Create a vector `my_lunch` of everything you ate (or will eat) for lunch
my_lunch <- c("ham and cheese sandwich", "chips")
meals <- list(my_breakfast, my_lunch)
meals$my_dinner <- c("broccoli, steak, corn")
dinner <- meals$my_dinner
my_lunch <- meals[[2]]
meals[5] <- my_lunch
meals[5] <- my_lunch
meals <- list(breakfast = my_breakfast, lunch = my_lunch)
meals$my_dinner <- c("broccoli, steak, corn")
meals$dinner <- c("broccoli, steak, corn")
meals <- list(breakfast = my_breakfast, lunch = my_lunch)
meals$dinner <- c("broccoli, steak, corn")
meals <- list(breakfast = my_breakfast, lunch = my_lunch)
print(meals)
meals$dinner <- c("broccoli, steak, corn")
print(meals)
meals$dinner <- c("broccoli", "steak", "corn")
print(meals)
meals[[5]] <- meals[["lunch"]]
# Use single-bracket notation to extract your breakfast and lunch from your list
# and save them to a list called `early_meals`
early_meals <- list(meals[1], meals[2])
# Use single-bracket notation to extract your breakfast and lunch from your list
# and save them to a list called `early_meals`
early_meals <- meals[c("breakfast", "lunch")]
# Create a list that has the number of items you ate for each meal
# Hint: use the `lappy()` function to apply the `length()` function to each item
num_of_breakfast <- length(meals$breakfast)
# Create a list that has the number of items you ate for each meal
# Hint: use the `lappy()` function to apply the `length()` function to each item
num_of_breakfast <- lapply(length(meals$breakfast))
# Create a list that has the number of items you ate for each meal
# Hint: use the `lappy()` function to apply the `length()` function to each item
num_of_breakfast <- lapply(meals, length())
# Create a list that has the number of items you ate for each meal
# Hint: use the `lappy()` function to apply the `length()` function to each item
num_of_breakfast <- lapply(meals, length
# Create a list that has the number of items you ate for each meal
# Hint: use the `lappy()` function to apply the `length()` function to each item
num_of_breakfast <- lapply(meals, length)
# Create a list that has the number of items you ate for each meal
# Hint: use the `lappy()` function to apply the `length()` function to each item
num_of_breakfast <- lapply(meals, length)
new_vector <- c(meal_vector, "pizza")
return(new_vector)
add_pizza(c("nachos"))
# Write a function `add_pizza` that adds pizza to a given meal vector, and
# returns the pizza-fied vector
add_pizza <- function(meal_vector) {
new_vector <- c(meal_vector, "pizza")
return(new_vector)
}
add_pizza(c("nachos"))
better_meals <- lapply(meals, add_pizza)
